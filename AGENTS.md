# Project Instructions

- Do not use CSS `clamp()`.
- Use a single fixed numeric value, normally in `px`, for CSS dimensions and font sizes.
- When different sizes are required for smaller screens, define explicit fixed values inside media queries.
